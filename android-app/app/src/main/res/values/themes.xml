<resources>
    <!-- Base application theme. -->
    <style name="Theme.NeoFitTV" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/settings_accent</item>
        <item name="colorPrimaryVariant">@color/settings_bg</item>
        <item name="colorOnPrimary">@android:color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/settings_accent</item>
        <item name="colorSecondaryVariant">@color/settings_bg</item>
        <item name="colorOnSecondary">@android:color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.NeoFitTV.FullScreen" parent="Theme.NeoFitTV">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <style name="Theme.SettingsTV" parent="Theme.NeoFitTV" />

</resources>
