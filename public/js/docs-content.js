window.DOCS_SECTIONS = [
  {
    id: 'technology',
    title: 'Схема работы',
    body: `
<h3>Что происходит при открытии плеера</h3>
<p>Пользователь открывает ссылку плеера (например с приставки или ТВ). Браузер загружает страницу <code>/player/index.html</code> и скрипт плеера. Плеер по параметру <code>id</code> в URL обращается к серверу: <strong>GET /api/player/:id</strong>. Сервер отдаёт JSON с данными экрана и плейлиста — список медиафайлов с путями и длительностями. Плеер запоминает этот плейлист и начинает воспроизведение. Параллельно регистрируется или обновляется <strong>Service Worker</strong> для кеширования.</p>

<h3>Скачивание и кеш в браузере</h3>
<p>Чтобы экран работал при обрыве интернета или недоступности сервера, медиафайлы и ответ API кешируются в браузере.</p>
<ul>
  <li><strong>Service Worker</strong> (файл <code>sw.js</code>) перехватывает запросы к медиа и к API плеера. При первом воспроизведении плейлиста файлы (картинки, видео) загружаются с сервера и сохраняются в кеш браузера.</li>
  <li>При следующих запросах к тем же URL браузер может отдать контент из кеша — без обращения к сети. Поэтому после однократной загрузки плейлист продолжает крутиться даже без интернета.</li>
  <li>Ответ API (состав плейлиста) кешируется по стратегии «сначала сеть»: при доступном сервере данные обновляются; при недоступности используется последний закешированный плейлист.</li>
</ul>
<p>Итог: один раз открыли плеер при работающей сети — контент «скачался» в кеш; дальше воспроизведение идёт с диска браузера и не зависит от связи.</p>

<h3>Воспроизведение</h3>
<p>Плеер по очереди показывает элементы плейлиста. Для картинок задаётся длительность в секундах; для видео — воспроизведение до конца. Таймер следит за сменой слайдов. Периодически (раз в 30 секунд по умолчанию) плеер снова запрашивает сервер и при изменении плейлиста подхватывает новую версию — смена контента происходит после текущего элемента, без перезагрузки страницы.</p>
<p><strong>Watchdog</strong> контролирует, что текущий элемент не «завис»: если картинка или видео не сменились в ожидаемое время (например, дольше чем длительность × 2), плеер принудительно переходит к следующему. Так снижается риск «залипания» из‑за сбоя загрузки или бага на устройстве.</p>

<h3>Автозапуск при включении консоли (приставки)</h3>
<p>Чтобы после подачи питания экран сам включался и показывал контент без участия человека:</p>
<ol>
  <li>В настройках приставки включается <strong>Auto Power On</strong> — устройство включается при появлении питания.</li>
  <li>Через приложение автозапуска (например, Launch on Boot) при загрузке Android запускается браузер со стартовой страницей, равной URL плеера.</li>
  <li>Браузер открывает плеер, тот запрашивает плейлист с сервера. Если сети нет — используется закешированный плейлист и медиа из кеша, воспроизведение начинается без интернета.</li>
</ol>
<p>Подробные шаги настройки — в разделе «Настройка Android приставки».</p>

<h3>Почему система не откажет</h3>
<ul>
  <li><strong>Нет сети или сервер недоступен</strong> — плеер работает из кеша: плейлист и медиа уже в браузере, показ продолжается.</li>
  <li><strong>Отключили электричество</strong> — приставка с Auto Power On и Launch on Boot снова включится и откроет плеер; при отсутствии сети контент подтянется из кеша.</li>
  <li><strong>Завис один ролик или картинка</strong> — watchdog через несколько секунд переключит на следующий элемент.</li>
  <li><strong>Браузер долго работал и начал тормозить</strong> — каждую ночь в 04:00 страница плеера перезагружается; кеш сохраняется, контент сразу снова доступен.</li>
</ul>
<p>Всё это делается без ручного вмешательства: кеш, автозапуск и watchdog заложены в технологию работы плеера и приставки.</p>
`
  },
  {
    id: 'android',
    title: 'Настройка Android приставки',
    body: `
<p>Чтобы экран работал автономно после отключения электричества, на Android-приставке нужно включить три вещи: автоматическое включение при подаче питания, автозапуск браузера с URL плеера и отключение перехода экрана в сон.</p>

<h3>Auto Power On (приставка включается сама)</h3>
<p>Приставка должна включаться при подаче питания, без ручного нажатия кнопки. Обычно эта опция находится в настройках Android.</p>
<ul>
  <li>Откройте <strong>Настройки</strong> (Settings).</li>
  <li>Перейдите в раздел <strong>Питание</strong> (Power) или <strong>Дополнительно</strong> (Additional settings).</li>
  <li>Включите опцию <strong>«Auto Power On»</strong>, <strong>«Включать при подаче питания»</strong> или аналог (название зависит от производителя: AMLogic, Rockchip, Allwinner и т.д.).</li>
</ul>
<p>После отключения электричества и его подачи приставка загрузится и включится без участия человека.</p>

<h3>Автозапуск браузера с URL плеера</h3>
<p>Чтобы после загрузки системы сразу открывался плеер NeoFit TV, используется приложение автозапуска.</p>
<ol>
  <li>Установите из Google Play приложение <strong>«Launch on Boot»</strong> (или аналог: «Start on Boot», «Boot Start» — в зависимости от доступности в вашем регионе).</li>
  <li>Включите главный переключатель в приложении (разрешить автозапуск).</li>
  <li>Выберите <strong>Chrome</strong> (или другой браузер) как приложение для запуска при загрузке.</li>
  <li>В настройках браузера установите <strong>стартовую страницу</strong> равной URL плеера. URL вы копируете в панели NeoFit TV в разделе «Экраны» — кнопка «Скопировать ссылку». Формат: <code>https://ваш-сервер:порт/player/index.html?id=ID_ЭКРАНА</code>.</li>
</ol>
<p>В итоге после включения приставки автоматически откроется браузер и загрузится страница плеера.</p>

<h3>Отключить сон экрана</h3>
<p>Экран не должен гаснуть или переходить в режим ожидания.</p>
<ul>
  <li>Откройте <strong>Настройки Android</strong> → <strong>Дисплей</strong> (Display).</li>
  <li>Найдите пункт <strong>«Отключение экрана»</strong>, <strong>«Screen timeout»</strong> или <strong>«Переход в сон»</strong>.</li>
  <li>Установите значение <strong>«Никогда»</strong> (Never) или максимально возможное.</li>
</ul>
<p>На некоторых приставках эта опция может быть в разделе «Энергосбережение» или «Для разработчиков».</p>

<div class="docs-note">Если приставка не из Play Маркета (китайская без GMS), автозапуск может настраиваться через сторонние лаунчеры или производитель уже вшил «киоскный» режим — см. инструкцию к устройству.</div>
`
  },
  {
    id: 'architecture',
    title: 'Техническая архитектура',
    body: `
<p>NeoFit TV построен по классической трёхзвенной схеме: клиент (браузер) обращается к HTTP API, сервер обрабатывает запрос через сервисный слой и сохраняет данные в файлы на диске.</p>

<h3>Общая схема потоков данных</h3>
<pre><code>Браузер администратора  →  HTTP API (Express.js)  →  Сервисный слой  →  Репозитории  →  JSON-файлы на диске
Браузер плеера         →  GET /api/player/:id   →  Сервисный слой  →  Репозитории  →  JSON-файлы на диске</code></pre>

<p>Админка и плеер не обращаются к файлам напрямую — только через REST API. Все изменения данных проходят через сервер.</p>

<h3>Три слоя бэкенда</h3>
<p>Разделение ответственности строгое:</p>

<p><strong>Маршруты (routes)</strong> — точка входа HTTP. Принимают запрос, проверяют авторизацию (если нужно), читают параметры и тело запроса, вызывают один или несколько методов сервиса, формируют JSON-ответ и код состояния. Не содержат бизнес-логики: не решают «можно ли удалить плейлист», не вычисляют «онлайн ли экран» — только передают данные в сервис и отдают результат.</p>

<p><strong>Сервисы (services)</strong> — вся бизнес-логика. Проверки (например, «файл не используется в плейлистах»), расчёты (например, isOnline по lastSeenAt и интервалу опроса), оркестрация вызовов репозиториев. Сервисы не знают про HTTP (ни req/res, ни коды ответов), не знают про формат хранения (JSON, БД) — только про доменные сущности и правила.</p>

<p><strong>Репозитории (repositories)</strong> — единственный слой, который читает и пишет данные. Работают с конкретным хранилищем (сейчас — JSON-файлы в <code>data/</code>). Замена JSON на базу данных потребует изменений только в репозиториях; маршруты и сервисы остаются теми же.</p>

<p>Такое разделение упрощает тестирование, замену хранилища и поддержку кода.</p>
`
  },
  {
    id: 'file-structure',
    title: 'Структура файлов',
    body: `
<p>Ниже перечислены ключевые файлы и папки проекта NeoFit TV и их назначение.</p>

<h3>Точка входа</h3>
<p><code>server.js</code> — создаёт Express-приложение, подключает middleware (helmet, json, session, static), регистрирует все роуты, запускает HTTP-сервер. Сам не содержит бизнес-логики.</p>

<h3>Конфигурация</h3>
<p><code>src/config/index.js</code> — единственное место, где читаются переменные окружения (<code>process.env</code>). Все остальные модули импортируют конфиг отсюда и не обращаются к env напрямую. Добавление новой настройки — только сюда.</p>

<h3>Middleware</h3>
<ul>
  <li><code>src/middleware/auth.js</code> — функция <code>requireAuth</code>: проверяет <code>req.session.authenticated</code>. Вешается на все защищённые /api/* роуты (кроме /api/auth/login и /api/player/*).</li>
  <li><code>src/middleware/rateLimit.js</code> — ограничение попыток входа: не более 10 запросов с одного IP за 15 минут. При превышении — 429.</li>
  <li><code>src/middleware/errorHandler.js</code> — глобальный обработчик ошибок. Ловит исключения, не пойманные роутами, логирует их, возвращает клиенту 500 (без внутренних деталей в продакшне).</li>
</ul>

<h3>Модуль Auth</h3>
<ul>
  <li><code>src/modules/auth/auth.routes.js</code> — POST /api/auth/login (вход), POST /api/auth/logout (выход), PUT /api/auth/password (смена пароля).</li>
  <li><code>src/modules/auth/auth.service.js</code> — <code>verifyPassword()</code> (сравнение с bcrypt-хешем), <code>changePassword()</code> (проверка текущего, хеширование нового, сохранение).</li>
  <li><code>src/modules/auth/auth.repository.js</code> — чтение/запись <code>data/auth.json</code>. Хранится только bcrypt-хеш пароля.</li>
</ul>

<h3>Модуль Media</h3>
<ul>
  <li><code>src/modules/media/media.routes.js</code> — GET /api/media (список), POST /api/media (загрузка), DELETE /api/media/:id, GET /api/media/:id/status.</li>
  <li><code>src/modules/media/media.service.js</code> — логика загрузки (проверка MIME по сигнатуре, безопасное имя, запуск оптимизации), удаления (проверка использования в плейлистах).</li>
  <li><code>src/modules/media/media.repository.js</code> — чтение/запись <code>data/media.json</code>.</li>
  <li><code>src/modules/media/media.processor.js</code> — оптимизация изображений (sharp), постановка видео в очередь (ffmpeg). Остальной код не зависит от sharp/ffmpeg напрямую.</li>
  <li><code>src/modules/media/video.queue.js</code> — in-memory очередь обработки видео, один поток. Состояние сохраняется в <code>data/processing-queue.json</code> — переживает рестарт сервера.</li>
</ul>

<h3>Модуль Playlists</h3>
<ul>
  <li><code>src/modules/playlists/playlists.routes.js</code> — CRUD для плейлистов (GET/POST /api/playlists, GET/PUT/DELETE /api/playlists/:id).</li>
  <li><code>src/modules/playlists/playlists.service.js</code> — валидация, проверка при удалении (не назначен на экраны), проверка существования mediaId при создании/обновлении.</li>
  <li><code>src/modules/playlists/playlists.repository.js</code> — чтение/запись <code>data/playlists.json</code>.</li>
</ul>

<h3>Модуль Screens</h3>
<ul>
  <li><code>src/modules/screens/screens.routes.js</code> — CRUD для экранов. GET списка возвращает объекты с полем <code>isOnline</code>.</li>
  <li><code>src/modules/screens/screens.service.js</code> — CRUD-логика, вычисление <code>isOnline</code>: (Date.now() - lastSeenAt) &lt;= pollInterval × 2.5 × 1000.</li>
  <li><code>src/modules/screens/screens.repository.js</code> — чтение/запись <code>data/screens.json</code>, метод <code>updateLastSeen(screenId)</code> для heartbeat от плеера.</li>
</ul>

<h3>Модуль Player</h3>
<ul>
  <li><code>src/modules/player/player.routes.js</code> — GET /api/player/:screenId (публичный, без авторизации).</li>
  <li><code>src/modules/player/player.service.js</code> — получение экрана по ID, загрузка плейлиста с медиа, обновление lastSeenAt, сборка объекта для плеера.</li>
</ul>

<h3>Модуль Settings</h3>
<ul>
  <li><code>src/modules/settings/settings.routes.js</code> — GET/PUT /api/settings.</li>
  <li><code>src/modules/settings/settings.service.js</code> — валидация значений (диапазоны, типы), partial update.</li>
  <li><code>src/modules/settings/settings.repository.js</code> — чтение/запись <code>data/settings.json</code>.</li>
</ul>

<h3>Утилиты</h3>
<ul>
  <li><code>src/utils/logger.js</code> — логирование с уровнями (info, warn, error), timestamp, в продакшне — в файл.</li>
  <li><code>src/utils/fileUtils.js</code> — <code>sanitizeFilename()</code>, <code>isAllowedMimeType()</code>, <code>getMimeType()</code>, <code>isPathSafe()</code> для безопасной работы с файлами.</li>
</ul>

<h3>Фронтенд — админка</h3>
<p><code>public/admin/index.html</code>, <code>media.html</code>, <code>playlists.html</code>, <code>screens.html</code>, <code>settings.html</code>, <code>docs.html</code> — страницы панели. <code>public/login.html</code> — форма входа. <code>public/js/api.js</code> — общие fetch-обёртки. <code>public/js/admin-media.js</code>, <code>admin-playlists.js</code>, <code>admin-screens.js</code>, <code>admin-settings.js</code> — логика соответствующих страниц.</p>

<h3>Фронтенд — плеер</h3>
<p><code>public/player/index.html</code> — HTML-оболочка плеера. <code>public/js/player.js</code> — воспроизведение, watchdog, автоперезагрузка в 04:00. <code>public/player/sw.js</code> — Service Worker: кэш медиа, офлайн-режим.</p>

<h3>Данные на диске</h3>
<p><code>data/auth.json</code> — хеш пароля. <code>data/media.json</code> — метаданные медиафайлов. <code>data/playlists.json</code>, <code>data/screens.json</code>, <code>data/settings.json</code> — плейлисты, экраны, настройки. <code>data/processing-queue.json</code> — очередь видео (автогенерируется). <code>uploads/</code> — каталог с загруженными файлами.</p>
`
  },
  {
    id: 'security',
    title: 'Безопасность',
    body: `
<p>В NeoFit TV реализованы базовые меры защиты: безопасное хранение пароля, защищённые сессии, ограничение перебора, проверка загружаемых файлов и раздачи статики, изоляция API и секретов.</p>

<h3>Аутентификация</h3>
<ul>
  <li>Пароль хранится только в виде <strong>bcrypt-хеша</strong> (cost factor 10). Даже при полном доступе к серверу восстановить пароль по хешу нельзя.</li>
  <li>Сессия передаётся в <strong>httpOnly</strong> cookie — JavaScript на странице не может прочитать или украсть её через XSS.</li>
  <li>У cookie установлен <strong>SameSite=Lax</strong> — снижается риск CSRF-атак (запросы с других сайтов не отправляют cookie при cross-site POST).</li>
  <li>В production для cookie включается флаг <strong>secure</strong> (передача только по HTTPS).</li>
</ul>

<h3>Rate limiting</h3>
<p>На endpoint входа действует ограничение: <strong>не более 10 попыток с одного IP за 15 минут</strong>. При превышении возвращается 429 без пояснений. Это делает перебор пароля по словарю практически нереалистичным.</p>

<h3>Загрузка файлов</h3>
<ul>
  <li><strong>MIME-тип</strong> проверяется по magic bytes (первые байты файла), а не по расширению. Переименование .exe в .jpg не позволит загрузить исполняемый файл.</li>
  <li>Допускаются только типы из <strong>явного whitelist</strong>: изображения (JPEG, PNG, GIF, WebP) и видео (MP4, WebM).</li>
  <li>Имя файла на диске формируется как <code>{uuid}_{sanitized_original}</code>. В имени остаются только буквы, цифры, точка, дефис, подчёркивание; последовательности вроде <code>../../../etc/passwd</code> исключаются.</li>
</ul>

<h3>Раздача файлов</h3>
<p>Перед отдачей файла из <code>uploads/</code> проверяется, что итоговый путь разрешён (начинается с UPLOADS_DIR). Path traversal через подмену URL невозможен.</p>

<h3>API</h3>
<ul>
  <li>Все маршруты <strong>/api/*</strong> требуют авторизованной сессии, кроме <strong>/api/auth/login</strong> (вход) и <strong>/api/player/*</strong> (публичный плеер).</li>
  <li>Ответы API — <strong>application/json</strong>. В ответах не отдаётся HTML, что снижает риск инъекций и путаницы с контентом.</li>
  <li>Секреты (SESSION_SECRET, пароль по умолчанию) берутся только из <strong>переменных окружения</strong>. В коде нет хардкода паролей, ключей, токенов.</li>
</ul>

<h3>Логирование безопасности</h3>
<p>Каждая попытка входа (успешная и неудачная) логируется с IP и временем. Удаление медиафайлов и плейлистов также логируется. Ошибки 5xx логируются с полным стектрейсом для диагностики без раскрытия деталей клиенту.</p>
`
  },
  {
    id: 'resilience',
    title: 'Отказоустойчивость',
    body: `
<p>NeoFit TV спроектирован так, чтобы сервер и плееры продолжали работать при сбоях сети, отключении питания и ошибках в контенте. Ниже — меры на стороне сервера и плеера.</p>

<h3>Сервер (Node.js/Express)</h3>
<ul>
  <li><strong>Глобальные обработчики</strong> <code>unhandledRejection</code> и <code>uncaughtException</code>: при необработанном исключении или отклонённом промисе ошибка логируется, через 1 секунду процесс завершается с кодом 1. Systemd (или другой супервизор) перезапускает сервис — не остаётся «висящего» процесса в неопределённом состоянии.</li>
  <li><strong>Очередь оптимизации видео</strong> при рестарте сервера восстанавливается из <code>data/processing-queue.json</code>. Незавершённые задачи продолжают обрабатываться после запуска.</li>
  <li><strong>Проверка свободного места на диске</strong> перед загрузкой файла. При менее 2 ГБ свободного места возвращается 507 Insufficient Storage с понятным сообщением.</li>
</ul>

<h3>Хранилище данных</h3>
<p>JSON-файлы в <code>data/</code> не требуют отдельного процесса БД. При старте сервера отсутствующие файлы создаются с дефолтными значениями — ручная инициализация не нужна.</p>

<h3>Плеер — Watchdog-таймер</h3>
<p>Следит за тем, чтобы элементы плейлиста переключались вовремя. Если текущий элемент (картинка или видео) «завис» дольше чем <code>duration × 2</code> (для видео — разумный запас), плеер принудительно переходит к следующему. Это защищает от зависших видео, незагрузившихся картинок и сбоев на Android-устройствах.</p>

<h3>Плеер — Service Worker и офлайн-кэш</h3>
<ul>
  <li>Медиафайлы текущего плейлиста кэшируются в браузере. При потере интернета плеер продолжает воспроизводить контент из кэша.</li>
  <li>Ответы API (плейлист) кэшируются по стратегии Network First — при недоступности сервера используется последний известный плейлист.</li>
  <li>После восстановления сети плеер снова опрашивает сервер и подтягивает изменения.</li>
</ul>

<h3>Плеер — обработка ошибок медиа</h3>
<p>Картинка не загрузилась — ожидание 2 секунды, затем переход к следующему. Видео зависло (событие stalled) — ожидание 3 секунды, переход к следующему. Пропуски логируются в консоль браузера для диагностики.</p>

<h3>Плеер — автоперезагрузка в 04:00</h3>
<p>Каждую ночь в 04:00 страница плеера перезагружается. Это снижает накопление утечек памяти браузером за длительную работу. Service Worker восстанавливает контент из кэша почти мгновенно — экран не темнеет, перерыв в показе минимален.</p>

<h3>Итоговая картина надёжности</h3>
<ul>
  <li>Сервер временно недоступен → плеер крутит кэш, работа продолжается.</li>
  <li>Интернет пропал → то же самое.</li>
  <li>Отключили электричество → приставка с Auto Power On включается сама, Chrome с Launch on Boot открывает плеер, Service Worker восстанавливает плейлист из кэша без обращения к серверу.</li>
  <li>Видео зависло → watchdog переключает на следующий элемент через несколько секунд.</li>
  <li>Браузер накопил утечки памяти → ночная перезагрузка в 04:00 снимает проблему.</li>
</ul>
`
  }
];
